CREATE TABLE IF NOT EXISTS `ApMetadata`
(
	`Id` INTEGER PRIMARY KEY AUTOINCREMENT,
	`Key` TEXT NOT NULL,
	`Value` TEXT
);

CREATE TABLE IF NOT	EXISTS [alThumbnail]
(
	[Id] INTEGER PRIMARY KEY AUTOINCREMENT,
	[ThumbnailType] INTEGER DEFAULT 1,
	[BitmapBytes] BLOB
);